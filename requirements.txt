soundfile==0.13.1
librosa==0.11.0
pyarrow==23.0.1
snac==1.2.1
transformers==4.57.6
speechbrain==1.1.0
onnxruntime==1.23.2
funasr==1.3.1
flask-sock==0.7.0
swanlab==0.7.11
# torch==2.6
# torchaudio==2.6
# torchvision==0.21.0

# ── QLoRA 微调（DeepSeek 等通用大模型）相关依赖 ──
# 注意：trl 1.4.0 源码包含 emoji 但未声明编码，Windows 上必须以 UTF-8 模式启动
# Python（PYTHONUTF8=1 或 python -X utf8）才能 import，详见
# docs/multi-model-training-plan.md §7.1
trl>=1.4.0
peft>=0.19.0
bitsandbytes>=0.49.0
datasets>=4.0.0
accelerate>=1.0.0